// Tests/LocalGravityTests/SmokeTests.swift
import XCTest
@testable import LocalGravity

final class SmokeTests: XCTestCase {
    func test_app_target_compiles() {
        XCTAssertTrue(true, "if this runs, the LocalGravity library builds")
    }
}
